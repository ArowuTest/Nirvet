openapi: 3.1.0
info:
  title: Nirvet SOC Platform API
  version: "0.1.0"
  description: >
    Cyber Security Operations Platform API (SOCaaS/MDR/XDR/Sovereign/MSSP).
    All endpoints except health, metrics, login and webhook ingestion require a
    Bearer JWT. Every request is tenant-scoped (RLS) and role-gated (RBAC).
servers:
  - url: http://localhost:8080
    description: Local
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    sourceKey:
      type: apiKey
      in: header
      name: X-Nirvet-Key
  schemas:
    Error:
      type: object
      properties:
        error:
          type: object
          properties:
            code: { type: string }
            message: { type: string }
    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email: { type: string, format: email }
        password: { type: string }
        mfa_code: { type: string, description: "required when the user has MFA enabled" }
    LoginResult:
      type: object
      properties:
        token: { type: string }
        user: { $ref: '#/components/schemas/User' }
    User:
      type: object
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        email: { type: string }
        role: { type: string }
        status: { type: string }
        mfa_enabled: { type: boolean }
    IngestInput:
      type: object
      required: [source]
      properties:
        source: { type: string }
        native_id: { type: string }
        class_name: { type: string }
        activity_name: { type: string }
        severity: { type: string, enum: [informational, low, medium, high, critical] }
        confidence: { type: integer }
        actor_ref: { type: string }
        target_ref: { type: string }
        action: { type: string }
        outcome: { type: string }
        data: { type: object, additionalProperties: true }
    Alert:
      type: object
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
        severity: { type: string }
        status: { type: string, enum: [new, assigned, closed, promoted] }
        source: { type: string }
        detection_id: { type: string, format: uuid }
        mitre: { type: array, items: { type: string } }
    Incident:
      type: object
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
        severity: { type: string }
        stage: { type: string, enum: [new, triage, investigating, contained, closed] }
        acknowledged_at: { type: string, format: date-time, nullable: true }
        ack_due_at: { type: string, format: date-time, nullable: true, description: SLA acknowledge deadline (per-severity) }
        resolve_due_at: { type: string, format: date-time, nullable: true, description: SLA resolve deadline (per-severity) }
        ack_breached: { type: boolean, description: derived — acknowledged late or still unacknowledged past due }
        resolve_breached: { type: boolean, description: derived — closed late or still open past due }
    Asset:
      type: object
      required: [ref, name]
      properties:
        id: { type: string, format: uuid }
        ref: { type: string, description: canonical reference matching actor_ref/target_ref (e.g. host:FIN-01) }
        name: { type: string }
        kind: { type: string, enum: [host, user, service, cloud, network, other] }
        criticality: { type: string, enum: [low, medium, high, critical] }
        owner: { type: string }
        tags: { type: array, items: { type: string } }
    Vulnerability:
      type: object
      required: [ref, title]
      properties:
        ref: { type: string, description: affected asset ref }
        cve: { type: string }
        title: { type: string }
        severity: { type: string, enum: [low, medium, high, critical] }
        cvss: { type: number }
        exploited: { type: boolean, description: known-exploited / active exploitation }
        status: { type: string, enum: [open, remediating, accepted, resolved] }
        remediation_due: { type: string, format: date-time, nullable: true }
    DetectionRule:
      type: object
      properties:
        name: { type: string }
        severity: { type: string }
        mitre: { type: array, items: { type: string } }
        condition:
          type: object
          properties:
            all: { type: array, items: { $ref: '#/components/schemas/Predicate' } }
            any: { type: array, items: { $ref: '#/components/schemas/Predicate' } }
    Predicate:
      type: object
      properties:
        field: { type: string }
        op: { type: string, enum: [eq, neq, contains, gte, lte, exists, regex] }
        value: { type: string }
    PlaybookRun:
      type: object
      properties:
        id: { type: string, format: uuid }
        status: { type: string, enum: [pending_approval, running, completed, failed, rejected] }
        steps: { type: array, items: { type: object } }
security:
  - bearerAuth: []
tags:
  - { name: health }
  - { name: auth }
  - { name: sso }
  - { name: mfa }
  - { name: admin }
  - { name: ingestion }
  - { name: detection }
  - { name: alerts }
  - { name: correlation }
  - { name: incidents }
  - { name: connectors }
  - { name: soar }
  - { name: ai }
  - { name: threat-intel }
  - { name: reporting }
  - { name: compliance }
  - { name: billing }
  - { name: ticketing }
paths:
  /healthz: { get: { tags: [health], security: [], summary: Liveness, responses: { '200': { description: ok } } } }
  /readyz: { get: { tags: [health], security: [], summary: Readiness (checks DB), responses: { '200': { description: ready }, '503': { description: db unavailable } } } }
  /metrics: { get: { tags: [health], security: [], summary: Prometheus metrics, responses: { '200': { description: metrics } } } }
  /auth/login:
    post:
      tags: [auth]
      security: []
      summary: Authenticate (password + optional MFA). Rate-limited per IP.
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/LoginRequest' } } } }
      responses:
        '200': { description: token issued, content: { application/json: { schema: { $ref: '#/components/schemas/LoginResult' } } } }
        '401': { description: invalid credentials/MFA, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '429': { description: rate limited }
  /auth/sso/start:
    get:
      tags: [sso]
      security: []
      summary: Begin OIDC login (302 to the tenant IdP). Rate-limited.
      parameters:
        - { name: connection, in: query, schema: { type: string, format: uuid }, description: SSO connection id }
        - { name: domain, in: query, schema: { type: string }, description: email domain (alternative to connection) }
      responses: { '302': { description: redirect to IdP }, '400': { description: missing connection/domain }, '404': { description: no connection } }
  /auth/sso/callback:
    get:
      tags: [sso]
      security: []
      summary: OIDC redirect callback — verifies id_token (JWKS), JIT-provisions, issues a session token
      parameters:
        - { name: code, in: query, required: true, schema: { type: string } }
        - { name: state, in: query, required: true, schema: { type: string } }
      responses:
        '200': { description: session issued, content: { application/json: { schema: { $ref: '#/components/schemas/LoginResult' } } } }
        '401': { description: token/id_token verification failed }
        '403': { description: email domain or tenant not permitted }
  /admin/sso:
    get: { tags: [sso], summary: List tenant SSO connections (tenant/platform admin), responses: { '200': { description: connections } } }
    post: { tags: [sso], summary: Create an OIDC connection (client secret sealed in vault), responses: { '201': { description: created } } }
  /admin/sso/{id}: { delete: { tags: [sso], summary: Delete an OIDC SSO connection, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: deleted } } } }
  /auth/sso/saml/start:
    get:
      tags: [sso]
      security: []
      summary: Begin SP-initiated SAML login (302 to the IdP with an AuthnRequest). Rate-limited.
      parameters:
        - { name: connection, in: query, schema: { type: string, format: uuid } }
        - { name: domain, in: query, schema: { type: string } }
      responses: { '302': { description: redirect to IdP }, '400': { description: missing connection/domain }, '404': { description: no connection } }
  /auth/sso/saml/acs:
    post:
      tags: [sso]
      security: []
      summary: SAML Assertion Consumer Service — validates the signed SAML Response (signature/conditions/audience/InResponseTo, all fail-closed) and issues a session
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema: { type: object, required: [SAMLResponse, RelayState], properties: { SAMLResponse: { type: string }, RelayState: { type: string } } }
      responses:
        '200': { description: session issued, content: { application/json: { schema: { $ref: '#/components/schemas/LoginResult' } } } }
        '401': { description: assertion validation failed }
        '403': { description: email domain not permitted }
  /admin/sso/saml:
    get: { tags: [sso], summary: List SAML connections (tenant/platform admin), responses: { '200': { description: connections } } }
    post: { tags: [sso], summary: Create a SAML connection (IdP entityID/SSO URL/cert + SP entityID/ACS), responses: { '201': { description: created } } }
  /admin/sso/saml/{id}: { delete: { tags: [sso], summary: Delete a SAML connection, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: deleted } } } }
  /me: { get: { tags: [auth], summary: Current user, responses: { '200': { description: user, content: { application/json: { schema: { $ref: '#/components/schemas/User' } } } } } } }
  /me/password: { post: { tags: [auth], summary: Change own password (verifies current), requestBody: { required: true, content: { application/json: { schema: { type: object, required: [current_password, new_password], properties: { current_password: { type: string }, new_password: { type: string, minLength: 8 } } } } } }, responses: { '200': { description: changed }, '401': { description: current password incorrect } } } }
  /mfa/enroll: { post: { tags: [mfa], summary: Begin TOTP enrollment (returns otpauth URI + secret once), responses: { '200': { description: enrolled } } } }
  /mfa/activate: { post: { tags: [mfa], summary: Activate MFA with a code, responses: { '200': { description: enabled }, '400': { description: invalid code } } } }
  /mfa/disable: { post: { tags: [mfa], summary: Disable MFA (verifies a code), responses: { '200': { description: disabled } } } }
  /admin/tenants:
    get: { tags: [admin], summary: List tenants (platform_admin), responses: { '200': { description: tenants } } }
    post: { tags: [admin], summary: Create tenant (platform_admin), responses: { '201': { description: created } } }
  /admin/tenants/{id}: { get: { tags: [admin], summary: Get tenant, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: tenant }, '404': { description: not found } } } }
  /admin/tenants/{id}/status: { post: { tags: [admin], summary: Guarded tenant status transition (platform_admin, TEN-004), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: tenant }, '400': { description: illegal transition } } } }
  /admin/tenants/{id}/profile:
    get: { tags: [admin], summary: Get tenant org profile (TEN-006), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: profile } } }
    put: { tags: [admin], summary: Update tenant org profile (timezone/business-hours/legal), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: profile } } }
  /admin/tenants/{id}/escalation-contacts:
    get: { tags: [admin], summary: List escalation matrix contacts (TEN-006), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: contacts } } }
    post: { tags: [admin], summary: Add an escalation contact, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '201': { description: created } } }
  /admin/tenants/{id}/escalation-contacts/{cid}: { delete: { tags: [admin], summary: Remove an escalation contact, parameters: [{ name: id, in: path, required: true, schema: { type: string } }, { name: cid, in: path, required: true, schema: { type: string } }], responses: { '200': { description: deleted } } } }
  /admin/tenants/{id}/authority-policies:
    get: { tags: [admin], summary: List authority-to-act policies (TEN-006/SOAR-003), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: policies } } }
    put: { tags: [admin], summary: Upsert an authority-to-act policy (fail-closed default), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: policy } } }
  /admin/tenants/{id}/history: { get: { tags: [admin], summary: Tenant change history (append-only, TEN-010), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: history } } } }
  /admin/tenants/{id}/service-accounts:
    get: { tags: [admin], summary: List service accounts (IAM-001), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: service accounts } } }
    post: { tags: [admin], summary: Create a service account (non-human principal; not platform_admin), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '201': { description: created } } }
  /admin/tenants/{id}/service-accounts/{sid}/keys:
    get: { tags: [admin], summary: List API key metadata (never the secret), parameters: [{ name: id, in: path, required: true, schema: { type: string } }, { name: sid, in: path, required: true, schema: { type: string } }], responses: { '200': { description: keys } } }
    post: { tags: [admin], summary: Mint an API key (raw key returned ONCE), parameters: [{ name: id, in: path, required: true, schema: { type: string } }, { name: sid, in: path, required: true, schema: { type: string } }], responses: { '201': { description: created with one-time key } } }
  /admin/tenants/{id}/api-keys/{kid}: { delete: { tags: [admin], summary: Revoke an API key (fail-closed on next use), parameters: [{ name: id, in: path, required: true, schema: { type: string } }, { name: kid, in: path, required: true, schema: { type: string } }], responses: { '200': { description: revoked } } } }
  /admin/tenants/{id}/session-policy:
    get: { tags: [admin], summary: Get session policy (TTL/IP allow-list/anomaly log, IAM-007), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: policy } } }
    put: { tags: [admin], summary: Update session policy (validated TTL + IP/CIDR allow-list), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: policy }, '400': { description: invalid TTL or allow-list entry } } }
  /me/elevations:
    get: { tags: [iam], summary: List my privileged elevations (IAM-004), responses: { '200': { description: elevations } } }
    post: { tags: [iam], summary: Request a time-bounded privileged elevation (awaits four-eyes approval), responses: { '201': { description: requested }, '400': { description: invalid target/boundary } } }
  /me/elevations/break-glass: { post: { tags: [iam], summary: Break-glass emergency elevation (immediate + alert + review-required, IAM-006), responses: { '201': { description: active } } } }
  /me/elevations/{id}/token: { post: { tags: [iam], summary: Mint a short-lived elevated token for an active elevation, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: token }, '409': { description: not active } } } }
  /admin/elevations:
    get: { tags: [admin], summary: List all privileged elevations (manager), responses: { '200': { description: elevations } } }
  /admin/elevations/{id}/approve: { post: { tags: [admin], summary: Approve a PAM elevation (four-eyes; requester != approver), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: active }, '403': { description: four-eyes violation }, '409': { description: not pending } } } }
  /admin/elevations/{id}/reject: { post: { tags: [admin], summary: Reject a PAM elevation, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: rejected } } } }
  /admin/elevations/{id}/review: { post: { tags: [admin], summary: Record post-use review of a break-glass elevation (IAM-006), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: reviewed } } } }
  /auth/invitations/accept: { post: { tags: [auth], summary: Accept an invitation and set a password (public, one-time, IAM-001/008), responses: { '201': { description: user activated }, '401': { description: invalid token }, '409': { description: expired/accepted/duplicate } } } }
  /admin/tenants/{id}/invitations:
    get: { tags: [admin], summary: List pending invitations, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: invitations } } }
    post: { tags: [admin], summary: Create a one-time invitation (token returned ONCE; role != platform_admin), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '201': { description: created with one-time token } } }
  /admin/tenants/{id}/invitations/{iid}: { delete: { tags: [admin], summary: Revoke a pending invitation, parameters: [{ name: id, in: path, required: true, schema: { type: string } }, { name: iid, in: path, required: true, schema: { type: string } }], responses: { '200': { description: revoked } } } }
  /admin/tenants/{id}/access-review: { get: { tags: [admin], summary: Access-review report — users/MFA/last-login + service accounts + invites + active elevations (IAM-009), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: report } } } }
  /admin/users: { post: { tags: [admin], summary: Create user (platform_admin), responses: { '201': { description: created } } } }
  /ingest:
    post:
      tags: [ingestion]
      summary: Ingest a security event (async normalize + detect)
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/IngestInput' } } } }
      responses: { '202': { description: accepted }, '400': { description: invalid }, '429': { description: quota exceeded } }
  /ingest/webhook/{id}:
    post:
      tags: [ingestion]
      security: [{ sourceKey: [] }]
      summary: Source-key authenticated webhook ingestion
      parameters: [{ name: id, in: path, required: true, schema: { type: string } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object, properties: { events: { type: array, items: { $ref: '#/components/schemas/IngestInput' } } } } } } }
      responses: { '202': { description: accepted }, '401': { description: bad key } }
  /events: { get: { tags: [ingestion], summary: Query normalized events, parameters: [{ name: severity, in: query, schema: { type: string } }, { name: search, in: query, schema: { type: string } }, { name: limit, in: query, schema: { type: integer } }], responses: { '200': { description: events } } } }
  /detections:
    get: { tags: [detection], summary: List detection rules, responses: { '200': { description: rules } } }
    post: { tags: [detection], summary: Create rule (detection_engineer), requestBody: { content: { application/json: { schema: { $ref: '#/components/schemas/DetectionRule' } } } }, responses: { '201': { description: created } } }
  /detections/import/sigma:
    post:
      tags: [detection]
      summary: Import a Sigma rule (YAML body) — translated to the native condition model
      requestBody: { required: true, content: { application/x-yaml: { schema: { type: string } } } }
      responses: { '201': { description: rule created, content: { application/json: { schema: { $ref: '#/components/schemas/DetectionRule' } } } }, '400': { description: unsupported/invalid Sigma } }
  /detections/{id}/enabled: { post: { tags: [detection], summary: Enable/disable a rule, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: ok } } } }
  /detections/cel:
    post:
      tags: [detection]
      summary: Create a CEL expression rule (compiled + type-checked; must yield bool)
      requestBody: { required: true, content: { application/json: { schema: { type: object, required: [name, severity, expression], properties: { name: { type: string }, description: { type: string }, severity: { type: string }, confidence: { type: integer }, mitre: { type: array, items: { type: string } }, expression: { type: string, example: 'event.severity == "critical" && event.class_name.contains("malware")' } } } } } }
      responses: { '201': { description: created }, '400': { description: invalid CEL expression } }
  /correlations: { get: { tags: [correlation], summary: Risk-ranked clusters of related alerts (§6.7), parameters: [{ name: status, in: query, schema: { type: string, enum: [open, promoted, closed] } }], responses: { '200': { description: correlations } } } }
  /correlations/{id}: { get: { tags: [correlation], summary: Get a correlation cluster, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: correlation }, '404': { description: not found } } } }
  /alerts: { get: { tags: [alerts], summary: List alerts, parameters: [{ name: status, in: query, schema: { type: string } }], responses: { '200': { description: alerts } } } }
  /alerts/{id}: { get: { tags: [alerts], summary: Get alert, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: alert, content: { application/json: { schema: { $ref: '#/components/schemas/Alert' } } } } } } }
  /alerts/{id}/assign: { post: { tags: [alerts], summary: Assign alert, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: assigned } } } }
  /alerts/{id}/promote: { post: { tags: [alerts], summary: Promote alert to incident, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '201': { description: incident } } } }
  /alerts/{id}/summarise: { post: { tags: [ai], summary: AI summary of an alert (assistive), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: summary } } } }
  /incidents: { get: { tags: [incidents], summary: List incidents, responses: { '200': { description: incidents } } } }
  /incidents/at-risk: { get: { tags: [incidents], summary: Open incidents breaching or near-breaching SLA (urgency-ordered), responses: { '200': { description: at-risk incidents } } } }
  /incidents/{id}/triage: { post: { tags: [incidents], summary: AI copilot triage assist (assistive-only, evidence-linked, offline fallback), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: assistive triage summary }, '404': { description: incident not found } } } }
  /incidents/{id}: { get: { tags: [incidents], summary: Get incident + timeline, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: incident } } } }
  /incidents/{id}/evidence-pack: { get: { tags: [incidents], summary: Export a tamper-evident evidence pack (case + timeline + alerts + events + assets + audit + SHA-256 manifest), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: evidence pack (downloadable JSON) }, '404': { description: incident not found } } } }
  /evidence/public-key: { get: { tags: [incidents], summary: Ed25519 public key that signs evidence packs (publish for out-of-band verification), responses: { '200': { description: public key } } } }
  /assets: { get: { tags: [assets], summary: List assets, responses: { '200': { description: assets } } }, post: { tags: [assets], summary: Register or update an asset (upsert on ref), requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/Asset' } } } }, responses: { '201': { description: asset } } } }
  /assets/{id}: { get: { tags: [assets], summary: Get an asset, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: asset }, '404': { description: not found } } } }
  /vulnerabilities: { get: { tags: [assets], summary: List vulnerabilities, parameters: [{ name: status, in: query, schema: { type: string } }, { name: ref, in: query, schema: { type: string } }], responses: { '200': { description: vulnerabilities } } }, post: { tags: [assets], summary: Register/update a vulnerability (upsert on ref+cve), requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/Vulnerability' } } } }, responses: { '201': { description: vulnerability } } } }
  /vulnerabilities/{id}: { get: { tags: [assets], summary: Get a vulnerability, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: vulnerability }, '404': { description: not found } } } }
  /exposure/summary: { get: { tags: [assets], summary: Open-vulnerability exposure rollup (by severity, exploited-open, past-due), responses: { '200': { description: exposure } } } }
  /entities/graph: { get: { tags: [entities], summary: Entity blast-radius graph — alerts, incidents, correlations and matched asset for a ref, parameters: [{ name: ref, in: query, required: true, schema: { type: string }, description: 'entity ref e.g. host:FIN-01 or user:jane@acme.com' }], responses: { '200': { description: entity graph }, '400': { description: ref required } } } }
  /incidents/{id}/assign: { post: { tags: [incidents], summary: Assign incident to an analyst (same-tenant), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], requestBody: { required: true, content: { application/json: { schema: { type: object, required: [assignee_id], properties: { assignee_id: { type: string, format: uuid } } } } } }, responses: { '200': { description: assigned }, '400': { description: invalid assignee } } } }
  /incidents/{id}/notes: { post: { tags: [incidents], summary: Add a note, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '201': { description: added } } } }
  /incidents/{id}/close: { post: { tags: [incidents], summary: Close incident, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: closed } } } }
  /connectors:
    get: { tags: [connectors], summary: List connectors, responses: { '200': { description: connectors } } }
    post: { tags: [connectors], summary: Create connector (webhook returns a one-time source key), responses: { '201': { description: created } } }
  /connectors/catalogue: { get: { tags: [connectors], summary: Available connector types, responses: { '200': { description: catalogue } } } }
  /connectors/{id}: { delete: { tags: [connectors], summary: Delete connector, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: deleted } } } }
  /playbooks: { get: { tags: [soar], summary: List playbooks, responses: { '200': { description: playbooks } } } }
  /playbooks/{id}/run: { post: { tags: [soar], summary: Run a playbook (authority-to-act gated), parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '201': { description: run, content: { application/json: { schema: { $ref: '#/components/schemas/PlaybookRun' } } } } } } }
  /soar/runs: { get: { tags: [soar], summary: List runs, responses: { '200': { description: runs } } } }
  /soar/runs/{id}: { get: { tags: [soar], summary: Get run, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: run } } } }
  /soar/runs/{id}/approve: { post: { tags: [soar], summary: Approve a pending run, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: completed } } } }
  /soar/runs/{id}/reject: { post: { tags: [soar], summary: Reject a pending run, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: rejected } } } }
  /soar/authority: { post: { tags: [soar], summary: Set tenant authority-to-act mode (platform_admin), responses: { '200': { description: set } } } }
  /threat-intel:
    get: { tags: [threat-intel], summary: List watchlist indicators, responses: { '200': { description: indicators } } }
    post: { tags: [threat-intel], summary: Add an indicator, responses: { '201': { description: added } } }
  /reports/summary: { get: { tags: [reporting], summary: Tenant operational summary, responses: { '200': { description: summary } } } }
  /compliance/coverage: { get: { tags: [compliance], summary: Framework control coverage (NIST CSF), parameters: [{ name: framework, in: query, schema: { type: string } }], responses: { '200': { description: coverage } } } }
  /billing/entitlements:
    get: { tags: [billing], summary: Get entitlements, responses: { '200': { description: entitlements } } }
    put: { tags: [billing], summary: Set entitlements (platform_admin), responses: { '200': { description: updated } } }
  /admin/ticketing:
    get: { tags: [ticketing], summary: List ITSM connections (tenant/platform admin), responses: { '200': { description: connections } } }
    post: { tags: [ticketing], summary: Create a ServiceNow/Jira connection (credential vault-sealed); incidents mirror to it on open, responses: { '201': { description: created } } }
  /admin/ticketing/{id}: { delete: { tags: [ticketing], summary: Delete an ITSM connection, parameters: [{ name: id, in: path, required: true, schema: { type: string } }], responses: { '200': { description: deleted } } } }

  # --- auto-added by cmd/openapigen (external-review OpenAPI parity); enrich schemas incrementally ---
  /admin/ai/allowed-endpoints:
    get:
      tags: [admin]
      summary: GET /admin/ai/allowed-endpoints
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    post:
      tags: [admin]
      summary: POST /admin/ai/allowed-endpoints
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/ai/allowed-endpoints/{id}:
    delete:
      tags: [admin]
      summary: DELETE /admin/ai/allowed-endpoints/{id}
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/ai/provider:
    get:
      tags: [admin]
      summary: GET /admin/ai/provider
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    put:
      tags: [admin]
      summary: PUT /admin/ai/provider
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/billing/accounts:
    post:
      tags: [admin]
      summary: POST /admin/billing/accounts
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/billing/accounts/{id}/invoice:
    get:
      tags: [admin]
      summary: GET /admin/billing/accounts/{id}/invoice
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/billing/accounts/{id}/suspend:
    post:
      tags: [admin]
      summary: POST /admin/billing/accounts/{id}/suspend
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/billing/packages:
    post:
      tags: [admin]
      summary: POST /admin/billing/packages
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/billing/packages/{id}/rates:
    post:
      tags: [admin]
      summary: POST /admin/billing/packages/{id}/rates
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/branding:
    put:
      tags: [admin]
      summary: PUT /admin/branding
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/flags:
    put:
      tags: [admin]
      summary: PUT /admin/flags
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/flags/rollback:
    post:
      tags: [admin]
      summary: POST /admin/flags/rollback
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/maintenance-windows:
    post:
      tags: [admin]
      summary: POST /admin/maintenance-windows
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/oversight/org-grants:
    delete:
      tags: [admin]
      summary: DELETE /admin/oversight/org-grants
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    post:
      tags: [admin]
      summary: POST /admin/oversight/org-grants
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/oversight/payer-grants:
    delete:
      tags: [admin]
      summary: DELETE /admin/oversight/payer-grants
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    post:
      tags: [admin]
      summary: POST /admin/oversight/payer-grants
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/syslog-sources:
    get:
      tags: [admin]
      summary: GET /admin/syslog-sources
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    post:
      tags: [admin]
      summary: POST /admin/syslog-sources
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/syslog-sources/{id}:
    delete:
      tags: [admin]
      summary: DELETE /admin/syslog-sources/{id}
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/syslog-sources/{id}/enabled:
    post:
      tags: [admin]
      summary: POST /admin/syslog-sources/{id}/enabled
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/tenants/batch:
    post:
      tags: [admin]
      summary: POST /admin/tenants/batch
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/tenants/{id}/ai-policy:
    put:
      tags: [admin]
      summary: PUT /admin/tenants/{id}/ai-policy
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/tenants/{id}/billing-mode:
    put:
      tags: [admin]
      summary: PUT /admin/tenants/{id}/billing-mode
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/tenants/{id}/billing-package:
    put:
      tags: [admin]
      summary: PUT /admin/tenants/{id}/billing-package
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/tenants/{id}/billing-suspend:
    post:
      tags: [admin]
      summary: POST /admin/tenants/{id}/billing-suspend
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/tenants/{id}/correlation-policy:
    get:
      tags: [admin]
      summary: GET /admin/tenants/{id}/correlation-policy
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    put:
      tags: [admin]
      summary: PUT /admin/tenants/{id}/correlation-policy
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/tenants/{id}/legal-hold:
    delete:
      tags: [admin]
      summary: DELETE /admin/tenants/{id}/legal-hold
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    post:
      tags: [admin]
      summary: POST /admin/tenants/{id}/legal-hold
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/tenants/{id}/mark-exported:
    post:
      tags: [admin]
      summary: POST /admin/tenants/{id}/mark-exported
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/tenants/{id}/offboard:
    post:
      tags: [admin]
      summary: POST /admin/tenants/{id}/offboard
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/tenants/{id}/sla-policies:
    get:
      tags: [admin]
      summary: GET /admin/tenants/{id}/sla-policies
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    put:
      tags: [admin]
      summary: PUT /admin/tenants/{id}/sla-policies
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/tenants/{id}/users/{uid}/disable:
    post:
      tags: [admin]
      summary: POST /admin/tenants/{id}/users/{uid}/disable
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: uid, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/tenants/{id}/users/{uid}/reactivate:
    post:
      tags: [admin]
      summary: POST /admin/tenants/{id}/users/{uid}/reactivate
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: uid, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /admin/tenants/{id}/users/{uid}/reset-password:
    post:
      tags: [admin]
      summary: POST /admin/tenants/{id}/users/{uid}/reset-password
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: uid, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /alerts/{id}/disposition:
    post:
      tags: [alerts]
      summary: POST /alerts/{id}/disposition
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /auth/password-reset/confirm:
    post:
      tags: [auth]
      summary: POST /auth/password-reset/confirm
      security: []
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
  /billing/invoice:
    get:
      tags: [billing]
      summary: GET /billing/invoice
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /billing/usage:
    get:
      tags: [billing]
      summary: GET /billing/usage
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /branding:
    get:
      tags: [branding]
      summary: GET /branding
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /compliance/controls:
    get:
      tags: [compliance]
      summary: GET /compliance/controls
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /compliance/frameworks:
    get:
      tags: [compliance]
      summary: GET /compliance/frameworks
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /compliance/status:
    put:
      tags: [compliance]
      summary: PUT /compliance/status
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /connectors/{id}/test:
    post:
      tags: [connectors]
      summary: POST /connectors/{id}/test
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /correlation-suppressions:
    get:
      tags: [correlation-suppressions]
      summary: GET /correlation-suppressions
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    post:
      tags: [correlation-suppressions]
      summary: POST /correlation-suppressions
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /correlation-suppressions/{id}:
    delete:
      tags: [correlation-suppressions]
      summary: DELETE /correlation-suppressions/{id}
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /correlations/metrics:
    get:
      tags: [correlations]
      summary: GET /correlations/metrics
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /correlations/storm:
    get:
      tags: [correlations]
      summary: GET /correlations/storm
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /correlations/{id}/explain:
    get:
      tags: [correlations]
      summary: GET /correlations/{id}/explain
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /correlations/{id}/override:
    put:
      tags: [correlations]
      summary: PUT /correlations/{id}/override
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /detections/coverage:
    get:
      tags: [detections]
      summary: GET /detections/coverage
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /detections/settings:
    get:
      tags: [detections]
      summary: GET /detections/settings
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    put:
      tags: [detections]
      summary: PUT /detections/settings
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /detections/tuning:
    get:
      tags: [detections]
      summary: GET /detections/tuning
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /detections/{id}/feedback:
    get:
      tags: [detections]
      summary: GET /detections/{id}/feedback
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /detections/{id}/metadata:
    put:
      tags: [detections]
      summary: PUT /detections/{id}/metadata
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /detections/{id}/rollback:
    post:
      tags: [detections]
      summary: POST /detections/{id}/rollback
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /detections/{id}/tests:
    get:
      tags: [detections]
      summary: GET /detections/{id}/tests
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    post:
      tags: [detections]
      summary: POST /detections/{id}/tests
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /detections/{id}/tests/run:
    post:
      tags: [detections]
      summary: POST /detections/{id}/tests/run
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /detections/{id}/tests/samples:
    post:
      tags: [detections]
      summary: POST /detections/{id}/tests/samples
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /detections/{id}/tests/{tid}:
    delete:
      tags: [detections]
      summary: DELETE /detections/{id}/tests/{tid}
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: tid, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /detections/{id}/transition:
    post:
      tags: [detections]
      summary: POST /detections/{id}/transition
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /detections/{id}/versions:
    get:
      tags: [detections]
      summary: GET /detections/{id}/versions
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /fleet/alerts:
    get:
      tags: [fleet]
      summary: GET /fleet/alerts
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /fleet/alerts/{id}/assign:
    post:
      tags: [fleet]
      summary: POST /fleet/alerts/{id}/assign
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /fleet/alerts/{id}/contain:
    post:
      tags: [fleet]
      summary: POST /fleet/alerts/{id}/contain
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /fleet/alerts/{id}/contain/{runID}/approve:
    post:
      tags: [fleet]
      summary: POST /fleet/alerts/{id}/contain/{runID}/approve
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: runID, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /fleet/alerts/{id}/contain/{runID}/reject:
    post:
      tags: [fleet]
      summary: POST /fleet/alerts/{id}/contain/{runID}/reject
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
        - { name: runID, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /fleet/alerts/{id}/disposition:
    post:
      tags: [fleet]
      summary: POST /fleet/alerts/{id}/disposition
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /incident-categories:
    get:
      tags: [incident-categories]
      summary: GET /incident-categories
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /incident-tasks/{id}:
    put:
      tags: [incident-tasks]
      summary: PUT /incident-tasks/{id}
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /incidents/{id}/attachments:
    get:
      tags: [incidents]
      summary: GET /incidents/{id}/attachments
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    post:
      tags: [incidents]
      summary: POST /incidents/{id}/attachments
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /incidents/{id}/category:
    put:
      tags: [incidents]
      summary: PUT /incidents/{id}/category
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /incidents/{id}/children:
    get:
      tags: [incidents]
      summary: GET /incidents/{id}/children
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /incidents/{id}/customer-timeline:
    get:
      tags: [incidents]
      summary: GET /incidents/{id}/customer-timeline
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /incidents/{id}/kb-links:
    get:
      tags: [incidents]
      summary: GET /incidents/{id}/kb-links
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    post:
      tags: [incidents]
      summary: POST /incidents/{id}/kb-links
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /incidents/{id}/major:
    put:
      tags: [incidents]
      summary: PUT /incidents/{id}/major
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /incidents/{id}/parent:
    post:
      tags: [incidents]
      summary: POST /incidents/{id}/parent
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /incidents/{id}/tasks:
    get:
      tags: [incidents]
      summary: GET /incidents/{id}/tasks
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    post:
      tags: [incidents]
      summary: POST /incidents/{id}/tasks
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /incidents/{id}/transition:
    post:
      tags: [incidents]
      summary: POST /incidents/{id}/transition
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /investigation/data-gaps:
    get:
      tags: [investigation]
      summary: GET /investigation/data-gaps
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /investigation/get-entity-graph:
    get:
      tags: [investigation]
      summary: GET /investigation/get-entity-graph
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /investigation/get-entity-profile:
    get:
      tags: [investigation]
      summary: GET /investigation/get-entity-profile
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /investigation/get-timeline:
    get:
      tags: [investigation]
      summary: GET /investigation/get-timeline
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /investigation/run-hunt-query:
    post:
      tags: [investigation]
      summary: POST /investigation/run-hunt-query
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /investigation/search-events:
    patch:
      tags: [investigation]
      summary: PATCH /investigation/search-events
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /knowledge-base:
    get:
      tags: [knowledge-base]
      summary: GET /knowledge-base
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    post:
      tags: [knowledge-base]
      summary: POST /knowledge-base
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /normalization/quality:
    get:
      tags: [normalization]
      summary: GET /normalization/quality
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /normalization/settings:
    get:
      tags: [normalization]
      summary: GET /normalization/settings
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    put:
      tags: [normalization]
      summary: PUT /normalization/settings
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /notify/inbox:
    get:
      tags: [notify]
      summary: GET /notify/inbox
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /notify/inbox/prefs:
    get:
      tags: [notify]
      summary: GET /notify/inbox/prefs
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    put:
      tags: [notify]
      summary: PUT /notify/inbox/prefs
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /notify/inbox/read-all:
    post:
      tags: [notify]
      summary: POST /notify/inbox/read-all
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /notify/inbox/unread-count:
    get:
      tags: [notify]
      summary: GET /notify/inbox/unread-count
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /notify/inbox/{id}/read:
    post:
      tags: [notify]
      summary: POST /notify/inbox/{id}/read
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /notify/links:
    post:
      tags: [notify]
      summary: POST /notify/links
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /notify/links/verify:
    get:
      tags: [notify]
      summary: GET /notify/links/verify
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /notify/senders:
    get:
      tags: [notify]
      summary: GET /notify/senders
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    put:
      tags: [notify]
      summary: PUT /notify/senders
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /notify/settings:
    put:
      tags: [notify]
      summary: PUT /notify/settings
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /notify/templates:
    get:
      tags: [notify]
      summary: GET /notify/templates
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    put:
      tags: [notify]
      summary: PUT /notify/templates
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /notify/test:
    post:
      tags: [notify]
      summary: POST /notify/test
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /posture/fleet:
    get:
      tags: [posture]
      summary: GET /posture/fleet
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /reports:
    post:
      tags: [reports]
      summary: POST /reports
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /reports/{id}:
    get:
      tags: [reports]
      summary: GET /reports/{id}
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /reports/{id}/download:
    get:
      tags: [reports]
      summary: GET /reports/{id}/download
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /soar/action-catalog:
    get:
      tags: [soar]
      summary: GET /soar/action-catalog
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    put:
      tags: [soar]
      summary: PUT /soar/action-catalog
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /soar/platform:
    get:
      tags: [soar]
      summary: GET /soar/platform
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    put:
      tags: [soar]
      summary: PUT /soar/platform
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /soar/runs/{id}/reverse:
    post:
      tags: [soar]
      summary: POST /soar/runs/{id}/reverse
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /soar/settings:
    get:
      tags: [soar]
      summary: GET /soar/settings
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    put:
      tags: [soar]
      summary: PUT /soar/settings
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /tenant/ai/provider:
    get:
      tags: [tenant]
      summary: GET /tenant/ai/provider
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    put:
      tags: [tenant]
      summary: PUT /tenant/ai/provider
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /threat-intel/settings:
    get:
      tags: [threat-intel]
      summary: GET /threat-intel/settings
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    put:
      tags: [threat-intel]
      summary: PUT /threat-intel/settings
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /threat-intel/stix:
    get:
      tags: [threat-intel]
      summary: GET /threat-intel/stix
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
    post:
      tags: [threat-intel]
      summary: POST /threat-intel/stix
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /threat-intel/stix/bundle:
    post:
      tags: [threat-intel]
      summary: POST /threat-intel/stix/bundle
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
  /threat-intel/stix/{id}:
    get:
      tags: [threat-intel]
      summary: GET /threat-intel/stix/{id}
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: OK }
        '400': { description: bad request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
        '401': { description: unauthenticated }
        '403': { description: forbidden }
